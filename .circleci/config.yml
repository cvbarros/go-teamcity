# Golang CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-go/ for more details
version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.13.9
    environment:
      GO111MODULE: "on"
      TEAMCITY_VERSION: "2019.2.2"
      CONTAINER_NAME: "teamcity_server"
      INTEGRATION_TEST_DIR: "integration_tests"
      TEAMCITY_DATA_DIR: "integration_tests/data_dir"
    working_directory: /go/src/github.com/{{ORG_NAME}}/{{REPO_NAME}}
    steps:
      - checkout
      - setup_remote_docker
      - run: |
            sh ./integration_tests/circleci_start_teamcity.sh
            TEAMCITY_ADDR="http://${DOCKER_HOST}:8112" go test -v -failfast -timeout 180s ./...